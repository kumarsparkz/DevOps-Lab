# DevOps Lab Environment Configuration
# Copy this file to .env and fill in the values
# NEVER commit the .env file to version control

# ============================================
# VMware REST API Credentials
# Used by: Init-LabHost.ps1, Terraform
# ============================================
VMWARE_API_USER=admin
VMWARE_API_PASSWORD=YourSecurePassword123!

# Terraform uses TF_VAR_ prefix for variables
TF_VAR_vmware_user=admin
TF_VAR_vmware_password=YourSecurePassword123!
TF_VAR_vmware_url=http://127.0.0.1:8697/api

# ============================================
# Windows/Ansible Credentials
# Used by: Ansible inventory, Packer
# ============================================
ANSIBLE_PASSWORD=YourSecurePassword123!

# ============================================
# SQL Server Credentials
# Used by: Ansible SQL role, smoke_test.ps1
# ============================================
SQL_SA_PASSWORD=YourSecureSqlPassword123!
SQL_SERVER_HOST=192.168.1.50

# ============================================
# Packer Variables
# Used by: packer build
# ============================================
PKR_VAR_winrm_password=YourSecurePassword123!
# Optional: Override ISO checksum if using a different ISO
# PKR_VAR_iso_checksum=sha256:YOUR_CHECKSUM_HERE

# ============================================
# Usage Instructions
# ============================================
# PowerShell (Windows):
#   Get-Content .env | ForEach-Object {
#     if ($_ -match '^([^#][^=]+)=(.*)$') {
#       [Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process')
#     }
#   }
#
# Bash (Linux/WSL):
#   export $(grep -v '^#' .env | xargs)
#
# Or use a tool like direnv for automatic loading
